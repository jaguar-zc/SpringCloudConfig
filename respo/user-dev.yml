spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/cloud-user
    username: postgres
    password: admin
    type: com.zaxxer.hikari.HikariDataSource